# config.py
import os
from dotenv import load_dotenv

# Завантаження змінних середовища з .env файлу
load_dotenv()

# Токен Telegram-бота
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")

# Мова за замовчуванням для транскрипції
DEFAULT_LANGUAGE = "auto"

# Стиль підсумовування за замовчуванням
DEFAULT_SUMMARY_STYLE = "default"

# Підтримувані мови для транскрипції та їх відображення
SUPPORTED_LANGUAGES = {
    "auto": "🌍 Автоматично",
    "uk": "🇺🇦 Українська",
    "ru": "💩 Російська",
    "en": "🇺🇸 Англійська",
    "de": "🇩🇪 Німецька",
    "fr": "🇫🇷 Французька",
    "es": "🇪🇸 Іспанська",
}

# Доступні стилі підсумовування тексту та відповідні підказки для LLM
SUMMARY_STYLES = {
    "default": {
        "name": "📝 Стандартний",
        "prompt": "Ти корисний асистент. Стисни текст користувача лаконічно та інформативно."
    },
    "short": {
        "name": "🤏 Дуже коротко (1-2 речення)",
        "prompt": "Ти корисний асистент. Стисни текст користувача в одне-два речення."
    },
    "bullet_points": {
        "name": "🔑 Ключові пункти",
        "prompt": "Ти корисний асистент. Стисло підсумуй текст користувача у вигляді списку з ключових пунктів."
    },
    "detailed": {
        "name": "🧐 Детально",
        "prompt": "Ти корисний асистент. Зроби детальний підсумок тексту користувача, зберігаючи ключову інформацію."
    },
    # Додайте інші стилі за потреби
}

# Розмір моделі Whisper для транскрипції (наприклад, 'base', 'small', 'medium', 'large')
# Чим більша модель, тим краща якість, але більший час завантаження та використання ресурсів.
WHISPER_MODEL_SIZE = os.getenv("WHISPER_MODEL_SIZE", "base")


# ----------------------------------------------------
# Додаткові налаштування для обробки файлів та LLM
# ----------------------------------------------------

# Максимальна довжина повідомлення в Telegram (4096 символів)
MAX_MESSAGE_LENGTH = 4096

# Розмір "чанку" для виводу довгих транскрипцій/підсумків
TRANSCRIPTION_DISPLAY_CHUNK_SIZE = 1000
SUMMARY_DISPLAY_CHUNK_SIZE = 1000 # Зазвичай дорівнює TRANSCRIPTION_DISPLAY_CHUNK_SIZE

# Максимальний розмір PDF-файлу для обробки (20 MB = 20 * 1024 * 1024 байт)
MAX_FILE_SIZE_PDF = 20 * 1024 * 1024

# Налаштування для рекурсивного саммарі
# Максимальна кількість символів на один чанк для відправки в LLM за раз
# Це значення потрібно підбирати, враховуючи ліміти токенів LLM (gpt-4o-mini ~16k токенів, 1 токен ~ 4 символи англійської, менше для інших мов)
MAX_CHARS_PER_CHUNK = 8000 

# Мінімальна кількість символів тексту, при якій починається рекурсивне саммарі
MIN_CHARS_FOR_RECURSIVE = 10000 

# Кількість символів, які будуть перекриватися між чанками при розбитті
# Допомагає зберегти контекст між частинами тексту
SUMMARY_CHUNK_OVERLAP = 500